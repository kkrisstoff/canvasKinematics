/*var ball = document.getElementById('ball');
getCoords(ball);
styling(ball);*/



function styling (el) {

}

var options = {
        type: 'ball'
    },
    ball = new AddNewItem(options);


addEvents(ball);